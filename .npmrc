## Project-local npmrc — RESET TO DEFAULT
## This project uses the default npm configuration. Keep this file empty
## or use it only for overrides that must apply inside this repo.
legacy-peer-deps=true